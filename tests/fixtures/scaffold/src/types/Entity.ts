/* Stub: Entity enum — superset of all entities used across test fixtures */
enum Entity {
  Article = 'article',
  Category = 'category',
  Item = 'item',
  Order = 'order',
  Product = 'product',
}

export default Entity;
