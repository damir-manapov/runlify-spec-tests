/* Stub: Entity enum — superset of all entities used across test fixtures */
enum Entity {
  Article = 'article',
  Category = 'category',
  Counter = 'counter',
  Entry = 'entry',
  Item = 'item',
  Order = 'order',
  Product = 'product',
  Ticket = 'ticket',
}

export default Entity;
