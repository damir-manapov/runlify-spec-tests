/* Stub: initEntities — not needed for tests, entities are managed via GraphQL API */
import {Context} from '../../adm/services/types';

const initEntities = async (_ctx: Context) => {
  // no-op in test scaffold
};

export default initEntities;
