/* Stub: initEndpoints */
import type { Express } from 'express'

const initEndpoints = async (
  _app: Express,
  _ctx: unknown,
  _port: number,
  _production: boolean,
) => {}

export default initEndpoints
